# FEM
Trying to implement Finite Element Method to solve PDEs

For Pixel Game Engine, you might need
```
sudo apt update
sudo apt install libglu1-mesa-dev libpng-dev
```

also, compile with the flags `-lX11 -lGL -lpthread -lpng -lstdc++fs` 
